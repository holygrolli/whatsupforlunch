name: augustiner

on:
  workflow_dispatch:
  schedule:
    - cron: '10 10 * * 0'

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  pipeline:
    uses: ./.github/workflows/location.yaml
    with:
      location: augustiner
    secrets: inherit
