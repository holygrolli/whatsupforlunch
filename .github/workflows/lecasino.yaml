name: lecasino

on:
  workflow_dispatch:
  schedule:
    - cron: '45 7 * * 1'

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  pipeline:
    uses: ./.github/workflows/location.yaml
    with:
      location: lecasino
    secrets: inherit
