name: ratskeller

on:
  workflow_dispatch:
  schedule:
    - cron: '0 14 * * 0'

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  pipeline:
    uses: ./.github/workflows/location.yaml
    with:
      location: ratskeller
    secrets: inherit
