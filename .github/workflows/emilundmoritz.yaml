name: emilundmoritz

on:
  workflow_dispatch:
  schedule:
    - cron: '10 5 * * 1'

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  pipeline:
    uses: ./.github/workflows/location.yaml
    with:
      location: emilundmoritz
    secrets: inherit
